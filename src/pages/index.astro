---
import Layout from '../layouts/Layout.astro';

// Informações dos ebooks disponíveis
const ebooks = [
  {
    id: 'pinns-petrofisica',
    title: 'PINNs em Petrofísica',
    subtitle: 'Integração entre Deep Learning e Equações de Rocha',
    description: 'Descubra como Physics-Informed Neural Networks (PINNs) estão revolucionando a análise petrofísica, combinando o poder do aprendizado profundo com as leis fundamentais da física de rochas.',
    cover: '/images/pinns-petrofisica/cover.png',
    price: 9999,
    highlights: [
      'Fundamentos de PINNs aplicados à petrofísica',
      'Modelagem com equações diferenciais parciais',
      'Casos práticos com dados reais',
      'Otimização e performance de modelos'
    ]
  },
  {
    id: 'integracao-de-metodos',
    title: 'Integração de Métodos Convencionais e Machine Learning',
    subtitle: 'Caracterização Petrofísica de Sistemas Fluviais',
    description: 'Estudo comparativo multi-escala entre métodos convencionais e Machine Learning para caracterização de fácies em sistemas fluviais complexos.',
    cover: '/images/integracao-de-metodos/cover.png',
    price: 10000,
    highlights: [
      'Comparação rigorosa entre métodos tradicionais e ML',
      'Análise de sistemas deposicionais fluviais',
      'Ferramentas de interpretabilidade avançadas',
      'Estudos de caso detalhados'
    ]
  }
];
---

<Layout title="Ebooks de Petrofísica - Machine Learning e Deep Learning">
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <div class="hero-text">
            <h1>Ebooks de Petrofísica</h1>
            <p class="subtitle">Machine Learning e Deep Learning Aplicados</p>
            <p class="description">
              Aprenda as técnicas mais modernas de caracterização petrofísica com nossos ebooks especializados.
              Combine conhecimento geológico com inteligência artificial para resultados superiores.
            </p>
            <div class="cta-section">
              <a href="#ebooks" class="btn-primary">
                Ver Ebooks Disponíveis
              </a>
              <p class="guarantee">✓ Acesso vitalício • ✓ Material interativo MDX</p>
            </div>
          </div>
          <div class="hero-image">
            <div class="books-showcase">
              <img src={ebooks[0].cover} alt={`Capa - ${ebooks[0].title}`} class="book-cover book-1" />
              <img src={ebooks[1].cover} alt={`Capa - ${ebooks[1].title}`} class="book-cover book-2" />
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Ebooks Disponíveis -->
    <section id="ebooks" class="ebooks-section">
      <div class="container">
        <h2>Nossos Ebooks</h2>
        <div class="ebooks-grid">
          {ebooks.map((ebook) => (
            <div class="ebook-card">
              <div class="ebook-image">
                <img src={ebook.cover} alt={`Capa - ${ebook.title}`} />
              </div>
              <div class="ebook-content">
                <h3>{ebook.title}</h3>
                <p class="ebook-subtitle">{ebook.subtitle}</p>
                <p class="ebook-description">{ebook.description}</p>
                <ul class="ebook-highlights">
                  {ebook.highlights.map(highlight => (
                    <li>✓ {highlight}</li>
                  ))}
                </ul>
                <div class="ebook-footer">
                  <div class="ebook-price">
                    <span class="price-value">{ebook.price.toLocaleString('pt-AO')} Kz</span>
                  </div>
                  <a href="/pagamento" class="btn-buy">Comprar Agora</a>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Tecnologias e Métodos -->
    <section class="learning">
      <div class="container">
        <h2>Tecnologias e Métodos Abordados</h2>
        <div class="learning-grid">
          <div class="learning-item">
            <div class="icon">🧠</div>
            <h3>Deep Learning e PINNs</h3>
            <p>Domine Physics-Informed Neural Networks e sua aplicação na integração de equações físicas com aprendizado profundo.</p>
          </div>
          <div class="learning-item">
            <div class="icon">📊</div>
            <h3>Machine Learning Comparativo</h3>
            <p>Compare métodos convencionais e técnicas de ML para caracterização de fácies e análise petrofísica.</p>
          </div>
          <div class="learning-item">
            <div class="icon">🔬</div>
            <h3>Caracterização de Reservatórios</h3>
            <p>Aplique técnicas avançadas em sistemas fluviais complexos e análise de propriedades de rochas.</p>
          </div>
          <div class="learning-item">
            <div class="icon">📈</div>
            <h3>Interpretabilidade e Performance</h3>
            <p>Utilize ferramentas de explicabilidade (SHAP) e otimize modelos para resultados superiores.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Sobre o autor -->
    <section class="author">
      <div class="container">
        <div class="author-content">
          <div class="author-info">
            <h2>Sobre o Autor</h2>
            <h3>Edmilson D. Praia</h3>
            <p>
              Especialista em Machine Learning aplicado à Petrofísica, com anos de experiência
              na indústria de óleo e gás. Edmilson combina conhecimento profundo em geociências
              com expertise em inteligência artificial para trazer soluções inovadoras ao setor.
            </p>
            <p>
              Seu trabalho pioneiro em Physics-Informed Neural Networks tem ajudado empresas
              a otimizar a caracterização de reservatórios e melhorar a precisão de modelos petrofísicos.
            </p>
          </div>
          <div class="author-image">
            <div class="author-placeholder">
              <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
                <circle cx="50" cy="50" r="48" fill="var(--bg-tertiary)" stroke="var(--secondary)" stroke-width="2"/>
                <text x="50" y="55" text-anchor="middle" fill="var(--secondary)" font-size="20" font-weight="bold">EP</text>
              </svg>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Final -->
    <section class="final-cta">
      <div class="container">
        <h2>Acelere Sua Expertise em Petrofísica</h2>
        <p>Tenha acesso imediato ao conteúdo completo após a confirmação do pagamento</p>
        <div class="cta-buttons">
          <a href="/pagamento" class="btn-primary btn-large">
            Escolher Ebook
          </a>
        </div>
        <div class="payment-methods">
          <p>Pagamento via Transferência Bancária • Confirmação em até 24h</p>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  main {
    padding-top: 80px;
  }

  .hero {
    padding: 3rem 0 4rem;
    background: var(--gradient-bg);
    border-bottom: 1px solid var(--border-color);
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    max-width: 1400px;
    margin: 0 auto;
  }

  .hero-text {
    text-align: left;
    padding: 0 2rem;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    margin-bottom: 1rem;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.2;
  }

  .subtitle {
    font-size: clamp(1.1rem, 3vw, 1.5rem);
    color: var(--secondary);
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  .description {
    font-size: clamp(1rem, 2vw, 1.1rem);
    color: var(--text-tertiary);
    margin-bottom: 2rem;
    line-height: 1.8;
  }

  .cta-section {
    margin-top: 2rem;
  }

  .price {
    margin-bottom: 1.5rem;
  }

  .price-label {
    display: block;
    font-size: clamp(0.9rem, 2vw, 1rem);
    color: var(--text-tertiary);
    margin-bottom: 0.5rem;
  }

  .price-value {
    display: block;
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    color: var(--text-primary);
  }

  .btn-primary {
    display: inline-block;
    background: var(--gradient-primary);
    color: white;
    padding: clamp(0.875rem, 2vw, 1rem) clamp(2rem, 4vw, 2.5rem);
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 600;
    font-size: clamp(1rem, 2vw, 1.1rem);
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 0 4px 20px var(--shadow-hover);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 30px var(--shadow-hover);
  }

  .btn-large {
    padding: clamp(1rem, 2.5vw, 1.25rem) clamp(2.5rem, 5vw, 3rem);
    font-size: clamp(1.1rem, 2.5vw, 1.25rem);
  }

  .guarantee {
    margin-top: 1rem;
    color: var(--text-tertiary);
    font-size: clamp(0.85rem, 2vw, 0.9rem);
  }

  .hero-image {
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
  }

  .books-showcase {
    position: relative;
    width: 100%;
    max-width: 500px;
    height: 400px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .book-cover {
    position: absolute;
    width: 280px;
    height: auto;
    border-radius: 0.75rem;
    box-shadow: 0 20px 60px var(--shadow-color);
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .book-1 {
    left: 0;
    z-index: 2;
    transform: rotateY(-15deg) rotateZ(-5deg);
    animation: float1 6s ease-in-out infinite;
  }

  .book-2 {
    right: 0;
    z-index: 1;
    transform: rotateY(15deg) rotateZ(5deg);
    animation: float2 6s ease-in-out infinite 1s;
  }

  .books-showcase:hover .book-1 {
    transform: rotateY(-10deg) rotateZ(-3deg) scale(1.05);
    box-shadow: 0 25px 70px var(--shadow-color);
  }

  .books-showcase:hover .book-2 {
    transform: rotateY(10deg) rotateZ(3deg) scale(1.05);
    box-shadow: 0 25px 70px var(--shadow-color);
  }

  @keyframes float1 {
    0%, 100% {
      transform: rotateY(-15deg) rotateZ(-5deg) translateY(0);
    }
    50% {
      transform: rotateY(-15deg) rotateZ(-5deg) translateY(-15px);
    }
  }

  @keyframes float2 {
    0%, 100% {
      transform: rotateY(15deg) rotateZ(5deg) translateY(0);
    }
    50% {
      transform: rotateY(15deg) rotateZ(5deg) translateY(-15px);
    }
  }

  /* Ebooks Section */
  .ebooks-section {
    padding: 4rem 0 5rem;
    background: var(--bg-primary);
  }

  .ebooks-section h2 {
    text-align: center;
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: 3rem;
    color: var(--text-primary);
  }

  .ebooks-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .ebook-card {
    background: var(--bg-secondary);
    border-radius: 1rem;
    border: 1px solid var(--border-color);
    overflow: hidden;
    transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s;
    display: flex;
    flex-direction: column;
  }

  .ebook-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 40px var(--shadow-color);
    border-color: var(--primary);
  }

  .ebook-image {
    height: 350px;
    overflow: hidden;
    background: var(--bg-tertiary);
  }

  .ebook-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ebook-content {
    padding: 2rem;
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .ebook-content h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.5rem);
    color: var(--text-primary);
    margin-bottom: 0.5rem;
    line-height: 1.3;
  }

  .ebook-subtitle {
    font-size: clamp(0.95rem, 2vw, 1rem);
    color: var(--secondary);
    margin-bottom: 1rem;
    line-height: 1.4;
  }

  .ebook-description {
    font-size: clamp(0.9rem, 2vw, 0.95rem);
    color: var(--text-tertiary);
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .ebook-highlights {
    list-style: none;
    padding: 0;
    margin: 0 0 1.5rem 0;
    flex: 1;
  }

  .ebook-highlights li {
    color: var(--text-secondary);
    font-size: clamp(0.85rem, 2vw, 0.9rem);
    margin-bottom: 0.75rem;
    padding-left: 1.5rem;
    position: relative;
  }

  .ebook-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: auto;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border-color);
  }

  .ebook-price .price-value {
    font-size: clamp(1.5rem, 3vw, 1.8rem);
    font-weight: 700;
    color: var(--primary);
  }

  .btn-buy {
    display: inline-block;
    background: var(--gradient-primary);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 600;
    font-size: clamp(0.95rem, 2vw, 1rem);
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
  }

  .btn-buy:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 25px rgba(59, 130, 246, 0.4);
  }

  .learning {
    padding: 4rem 0 5rem;
    background: var(--bg-primary);
  }

  .learning h2 {
    text-align: center;
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: 3rem;
    color: var(--text-primary);
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
    margin-bottom: 3rem;
  }

  .learning-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .learning-item {
    background: var(--bg-secondary);
    padding: 2rem;
    border-radius: 1rem;
    border: 1px solid var(--border-color);
    transition: transform 0.3s, border-color 0.3s, box-shadow 0.3s;
  }

  .learning-item:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
    box-shadow: 0 8px 24px var(--shadow-color);
  }

  .icon {
    font-size: clamp(2.5rem, 4vw, 3rem);
    margin-bottom: 1rem;
  }

  .learning-item h3 {
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .learning-item p {
    color: var(--text-tertiary);
    line-height: 1.6;
    font-size: clamp(0.9rem, 2vw, 1rem);
  }

  .author {
    padding: 4rem 0 5rem;
    background: var(--bg-secondary);
  }

  .author-content {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 3rem;
    align-items: center;
    max-width: 1000px;
    margin: 0 auto;
  }

  .author-info {
    text-align: left;
  }

  .author h2 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .author h3 {
    font-size: clamp(1.5rem, 3vw, 1.8rem);
    color: var(--secondary);
    margin-bottom: 1.5rem;
  }

  .author p {
    color: var(--text-tertiary);
    line-height: 1.8;
    margin-bottom: 1rem;
    font-size: clamp(0.95rem, 2vw, 1rem);
    max-width: 100%;
  }

  .author-placeholder {
    width: 100%;
    max-width: 200px;
    height: auto;
    aspect-ratio: 1;
    margin: 0 auto;
  }

  .author-placeholder svg {
    width: 100%;
    height: 100%;
  }

  .final-cta {
    padding: 4rem 0 5rem;
    background: var(--gradient-bg);
    text-align: center;
  }

  .final-cta h2 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .final-cta > .container > p {
    color: var(--text-tertiary);
    font-size: clamp(1rem, 2vw, 1.2rem);
    margin-bottom: 2rem;
  }

  .cta-buttons {
    margin-bottom: 1.5rem;
  }

  .payment-methods p {
    color: var(--text-tertiary);
    font-size: clamp(0.85rem, 2vw, 0.9rem);
  }

  /* Responsividade Avançada */
  @media (max-width: 1024px) {
    main {
      padding-top: 60px;
    }

    .hero-content {
      gap: 3rem;
    }

    .learning-grid {
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 2rem 0 3rem;
    }

    .hero-content,
    .author-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-image {
      order: -1;
    }

    .books-showcase {
      max-width: 400px;
      height: 300px;
    }

    .book-cover {
      width: 200px;
    }

    .ebooks-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .ebook-image {
      height: 300px;
    }

    .ebook-content {
      padding: 1.5rem;
    }

    .ebook-footer {
      flex-direction: column;
      gap: 1rem;
      align-items: flex-start;
    }

    .btn-buy {
      width: 100%;
      text-align: center;
    }

    .learning {
      padding: 3rem 0 4rem;
    }

    .learning-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .learning-item {
      padding: 1.5rem;
    }

    .author {
      padding: 3rem 0 4rem;
    }

    .author-content {
      text-align: center;
    }

    .author-info {
      text-align: center;
    }

    .final-cta {
      padding: 3rem 0 4rem;
    }

    .cta-buttons {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    .btn-primary {
      width: 100%;
      max-width: 300px;
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    main {
      padding-top: 50px;
    }

    .hero {
      padding: 1.5rem 0 2rem;
    }

    .books-showcase {
      max-width: 350px;
      height: 250px;
    }

    .book-cover {
      width: 160px;
    }

    .ebooks-section {
      padding: 2rem 0 3rem;
    }

    .ebook-image {
      height: 250px;
    }

    .ebook-content {
      padding: 1.25rem;
    }

    .learning {
      padding: 2rem 0 3rem;
    }

    .learning h2,
    .author h2,
    .final-cta h2 {
      margin-bottom: 2rem;
    }

    .author {
      padding: 2rem 0 3rem;
    }

    .final-cta {
      padding: 2rem 0 3rem;
    }
  }

  /* Landscape mobile */
  @media (max-height: 600px) and (orientation: landscape) {
    .hero {
      padding: 2rem 0;
    }

    .learning,
    .author,
    .final-cta {
      padding: 2rem 0;
    }
  }
</style>
